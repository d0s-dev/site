---
import MacTerminal from "./MacTerminal.astro";

const installCommands = [
  {
    input: "curl -sSL https://d0s.dev/install | sh",
    output: "✓ d0s v1.0.0 installed successfully",
  },
  {
    input: "d0s init",
    output: "🚀 Cluster initialized | Ready to deploy",
  },
  {
    input: "d0s deploy nginx:latest",
    output: "📦 Deploying nginx:latest\n✓ Package verified | 0 CVEs detected\n🚀 Live in 8s",
  },
];
---
<section class="relative px-4 py-12 md:px-8">
  <div class="absolute inset-0">
    <div class="absolute right-0 top-1/2 size-[500px] -translate-y-1/2 rounded-full bg-brand-blue-400/10 blur-3xl"></div>
  </div>
  
  <div class="relative mx-auto max-w-5xl">
    <div class="mb-8 text-center">
      <h2 class="text-3xl font-bold text-white sm:text-4xl">
        Get Started in <span class="gradient-text">Seconds</span>
      </h2>
      <p class="mx-auto mt-4 max-w-2xl text-lg text-brand-gray-200">
        Install d0s and deploy your first secure application in three simple commands
      </p>
    </div>

    <div class="mx-auto max-w-3xl">
      <MacTerminal 
        commands={installCommands} 
        loop={true}
        className="shadow-[0_30px_100px_rgba(3,83,164,0.35)]"
      />
    </div>

    <div class="mt-12 flex flex-wrap justify-center gap-4 text-sm">
      <div class="flex items-center gap-2 rounded-full border border-brand-blue-400/30 bg-card/50 px-4 py-2 text-brand-gray-100">
        <svg class="size-5 text-brand-blue-100" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        Self-Hosted Images
      </div>
      <div class="flex items-center gap-2 rounded-full border border-brand-blue-400/30 bg-card/50 px-4 py-2 text-brand-gray-100">
        <svg class="size-5 text-brand-blue-100" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>
        Works offline
      </div>
      <div class="flex items-center gap-2 rounded-full border border-brand-blue-400/30 bg-card/50 px-4 py-2 text-brand-gray-100">
        <svg class="size-5 text-brand-blue-100" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
        </svg>
        FIPS Compliant
      </div>
    </div>
  </div>
</section>
