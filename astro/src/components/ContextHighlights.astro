---
const highlights = [
  {
    icon: "📦",
    title: "Fully Bundled Apps",
    description: "More than just containers, we offer complete application packages ready to deploy",
  },
  {
    icon: "⚡",
    title: "Offline Helm w/Automation",
    description: "Helm charts with built-in automation for disconnected environments",
  },
  {
    icon: "🛡️",
    title: "Trusted FIPS Containers",
    description: "UBI-based FIPS containers from verified, trusted origins",
  },
  {
    icon: "🚀",
    title: "IDP in One Command",
    description: "Complete internal developer platform deployed with a single command",
  },
];
---
<section class="relative px-4 py-24 md:px-8">
  <div class="mx-auto max-w-7xl">
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      {highlights.map((highlight, index) => (
        <div 
          class="highlight-card group relative overflow-hidden rounded-xl border border-brand-blue-400/30 bg-gradient-to-br from-card/60 to-card/40 p-6 backdrop-blur transition-all duration-500 hover:-translate-y-1 hover:border-brand-blue-300 hover:shadow-[0_20px_60px_rgba(4,102,200,0.25)]"
          style={`animation-delay: ${index * 100}ms`}
        >
          <div class="mb-4 flex size-14 items-center justify-center rounded-2xl bg-gradient-to-br from-brand-blue-200/20 to-brand-blue-400/20 text-3xl transition-transform duration-300 group-hover:scale-110">
            {highlight.icon}
          </div>
          <h3 class="mb-3 text-xl font-semibold text-white">
            {highlight.title}
          </h3>
          <p class="text-sm leading-relaxed text-brand-gray-200">
            {highlight.description}
          </p>
          <div class="absolute inset-0 -z-10 opacity-0 transition-opacity duration-500 group-hover:opacity-100" 
               style="background: radial-gradient(circle at 50% 0%, rgba(4,102,200,0.15) 0%, transparent 70%);"></div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .highlight-card {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeInUp 0.6s ease forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
